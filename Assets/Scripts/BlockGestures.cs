using System.Collections;
using System.Collections.Generic;
using UnityEngine;
//using UnityEngine.InputSystem;

public class BlockGestures : MonoBehaviour
{
	// Флаг для отслеживания жеста свайпа вверх
	//private bool isSwipingUp = false;
	//private float swipePanelThreshold = 250f;
	//void Update()
	//{
	//    if(Input.touchCount > 0 && Input.GetTouch(0).phase == TouchPhase.Moved)
	//    {
	//        Vector2 touchDeltaPosition = Input.GetTouch(0).deltaPosition;
	//        if (touchDeltaPosition.y > swipePanelThreshold && Input.GetTouch(0).position.y < swipePanelThreshold)
	//        {
	//            // Блокируем свайп, предотвращая вызов системной панели
	//            Debug.Log("Swipe from bottom to top blocked.");
	//            return;
	//        }
	//    }
	//}
	//Проверяем, выполняется ли жест свайпа вверх
	//if (isSwipingUp)
	//{
	//    Здесь можно добавить логику, которая будет выполняться при свайпе вверх
	//    Debug.Log("Свайп вверх выполнен");
	//    isSwipingUp = false; // Сбрасываем флаг
	//}
	//if (Input.touchCount > 0)
	//{
	//    Touch touch = Input.GetTouch(0);
	//    if (touch.phase == TouchPhase.Moved)
	//    {

	//    }
	//}
}

    //public void OnSwipeUp(InputAction.CallbackContext context)
    //{
    //    // Если жест свайпа вверх начался, устанавливаем флаг
    //    if (context.started)
    //    {
    //        isSwipingUp = true;
    //    }
    //}

